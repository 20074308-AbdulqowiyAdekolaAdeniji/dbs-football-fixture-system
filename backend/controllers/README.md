# API Controllers

This folder contains business logic for API endpoints.

## Structure
- `fixtureController.js` - Handles fixture operations (CRUD)
- `venueController.js` - Handles venue operations (future)

## Pattern
Controllers contain the actual logic:
- Database queries
- Data validation
- Error handling
- Response formatting

Routes call controller functions.